apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-secrets
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  {{ range $k, $v := .Values.mysql.statefulSet.secrets }}
  {{ $k }}: {{ $v | b64enc }}
  {{ end }}